import Controller from "../vController";
import Image from "../vImage";

const state = { title: "title" };
const mutations = { }
const actions = { }

const modules = { Controller, Image };

export default { state,modules };

import Controller from '../vMolecule/controller'
import Image from '../vAtom/image'

const state = {

}
const getters = {
   
  }

const mutations = {
 
  }

  const actions = {}


export default {
  state,
  mutations,
  actions,
  getters,
  modules: {
    Controller,
    Image
  }
}

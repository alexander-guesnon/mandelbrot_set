import Vue from "vue"
import Vuex from "vuex"
import MB from "./modules/vOrganism/MB"
Vue.use(Vuex);

export default new Vuex.Store({
  state: { },
  mutations: {},
  actions: {},
  modules: { MB }
});

import Controller from '../vMolecule/store/controller'
import Image from '../vMolecule/vAtom/store/image'

const state = {
title: 'Mandle Brotset'
}
const getters = {
   
  }

const mutations = {
 
  }

  const actions = {}


export default {
  state,
  mutations,
  actions,
  getters,
  modules: {
    Controller,
    Image
  }
}

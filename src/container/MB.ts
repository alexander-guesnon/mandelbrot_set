const state = {
  title: "Mandelbrot Set",
}
const getters = {
    'title': ( state, getters) => { return state.title  }
  }

const mutations = {

}

const actions = {

}

export default {
  state,
  mutations,
  actions,
  getters
}
